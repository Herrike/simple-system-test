import MUIButton, { ButtonProps } from '@mui/material/Button'
import {type FC} from 'react'

const SearchButton:FC<ButtonProps> = (_props) => {
  return (
    <MUIButton>Button</MUIButton>
  )
}

export default SearchButton